project:
  name: trino-venice
  description: This is a Trino connector
  license: Apache-2
  java:
    groupId: dev.manujose0
    version: 11

files:
  artifacts:
    - path: "target/{{projectName}}-{{projectVersion}}.zip"

release:
  github:
    owner: trino-venice
